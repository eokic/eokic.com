html {
  @apply text-base text-slate-700;
  background: white;
  /* scroll-behavior: smooth; */
}

html,
body {
  max-width: 100vw;
  overflow-x: clip;
}

.container {
  @apply pr-safe-edge-r pl-safe-edge-l;
}
